import React from 'react'

export const BalanceGraph = ({ capPercent, startYear, endYear }) => {

    return (
        <div>
            <div>capPercent: {capPercent}</div>
            <div>startYear: {startYear}</div>
            <div>endYear: {endYear}</div>
        </div>
    );

};